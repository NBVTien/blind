Read all docs in the `docs/` directory and build a complete mental model of the project before doing any work.

Steps:
1. Read `docs/overview.md` — what the app is and who uses it
2. Read `docs/features.md` — full feature spec with UI details
3. Read `docs/data-model.md` — all shared types and their semantics
4. Read `docs/api.md` — all endpoints, bodies, storage details
5. Read `docs/design.md` — aesthetic direction, fonts, colors, layout rules
6. Read `docs/conventions.md` — coding conventions for web, api, and shared

After reading, summarize in one short paragraph: what changed recently vs what the core model is. Flag any doc sections that look stale or inconsistent with each other.
